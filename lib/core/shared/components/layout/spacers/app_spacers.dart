import 'package:flutter/material.dart';

class AppSpacers {
  static SizedBox verticalSpacer({double height = 16.0}) =>
      SizedBox(height: height);

  static SizedBox horizontalSpacer({double width = 16.0}) =>
      SizedBox(width: width);
}
